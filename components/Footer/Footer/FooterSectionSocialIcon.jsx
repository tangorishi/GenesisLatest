import { Icon } from '@chakra-ui/react';

const FooterSectionSocialIcon = ({
  icon,
}) => (
  <Icon
    as={icon}
    w={6}
    h={6}
    color="white"
    marginRight="10px"
  />
);

export default FooterSectionSocialIcon;
